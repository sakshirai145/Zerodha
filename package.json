{
  "name": "zerodha-clone",
  "version": "1.0.0",
  "private": true,
  "description": "Zerodha Clone - Full Stack MERN Application",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix dashboard",
    "build:frontend": "npm run build --prefix frontend",
    "build:dashboard": "npm run build --prefix dashboard",
    "build": "npm run build:frontend && npm run build:dashboard",
    "start": "npm start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "smoke-test": "node smoke-test.mjs"
  }
}
